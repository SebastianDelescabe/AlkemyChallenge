import { getInfo } from './getInfo';
import { getInfoById } from './getInfoById';
import { handleTotalPrice, handleRedyIn, handleHealtScore } from './logicPriceHealtMinutes';
import { logout } from './logout';
import {searchRecipes} from './searchRepices';

export {
    getInfo,
    getInfoById,
    handleTotalPrice,
    handleRedyIn,
    handleHealtScore,
    logout,
    searchRecipes
}